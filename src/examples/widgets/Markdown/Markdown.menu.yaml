kind: NavMenuItem
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-navmenuitem
  namespace: krateo-system
spec:
  widgetData:
    allowedResources:
      - pages
    resourceRefId: example-markdown-page
    label: Markdown
    icon: fa-quote-right
    path: /markdown
  resourcesRefs:
    items:
      - id: example-markdown-page
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-page
        namespace: krateo-system
        resource: pages
        verb: GET
---
kind: Page
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-page
  namespace: krateo-system
spec:
  widgetData:
    allowedResources:
      - barcharts
      - buttons
      - columns
      - datagrids
      - eventlists
      - filters
      - flowcharts
      - forms
      - linecharts
      - markdowns
      - panels
      - paragraphs
      - piecharts
      - rows
      - tables
      - tablists
      - yamlviewers
    items:
      - resourceRefId: example-markdown-empty-panel
      - resourceRefId: example-markdown-basic-panel
      - resourceRefId: example-markdown-copy-panel
      - resourceRefId: example-markdown-download-panel
      - resourceRefId: example-markdown-download-md-panel
      - resourceRefId: example-markdown-combined-panel
      - resourceRefId: example-markdown-long-panel
      - resourceRefId: example-markdown-table-panel
  resourcesRefs:
    items:
      - id: example-markdown-empty-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-empty-panel
        namespace: krateo-system
        resource: panels
        verb: GET  
      - id: example-markdown-basic-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-basic-panel
        namespace: krateo-system
        resource: panels
        verb: GET  
      - id: example-markdown-copy-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-copy-panel
        namespace: krateo-system
        resource: panels
        verb: GET      
      - id: example-markdown-download-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-download-panel
        namespace: krateo-system
        resource: panels
        verb: GET       
      - id: example-markdown-download-md-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-download-md-panel
        namespace: krateo-system
        resource: panels
        verb: GET
      - id: example-markdown-combined-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-combined-panel
        namespace: krateo-system
        resource: panels
        verb: GET
      - id: example-markdown-long-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-long-panel
        namespace: krateo-system
        resource: panels
        verb: GET
      - id: example-markdown-table-panel
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-table-panel
        namespace: krateo-system
        resource: panels
        verb: GET
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-empty-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Empty markdown'
    items:
      - resourceRefId: example-markdown-empty
  resourcesRefs:
    items:
      - id: example-markdown-empty
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-empty
        namespace: krateo-system
        resource: markdowns
        verb: GET  
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-basic-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Basic markdown formatting'
    items:
      - resourceRefId: example-markdown-basic
  resourcesRefs:
    items:
      - id: example-markdown-basic
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-basic
        namespace: krateo-system
        resource: markdowns
        verb: GET
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-copy-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Allow copy enabled'
    items:
      - resourceRefId: example-markdown-copy
  resourcesRefs:
    items:
      - id: example-markdown-copy
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-copy
        namespace: krateo-system
        resource: markdowns
        verb: GET
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-download-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Allow download enabled'
    items:
      - resourceRefId: example-markdown-download
  resourcesRefs:
    items:
      - id: example-markdown-download
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-download
        namespace: krateo-system
        resource: markdowns
        verb: GET  
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-download-md-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Custom file extension for download (.md)'
    items:
      - resourceRefId: example-markdown-download-md
  resourcesRefs:
    items:
      - id: example-markdown-download-md
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-download-md
        namespace: krateo-system
        resource: markdowns
        verb: GET 
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-combined-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Combined actions (copy + download)'
    items:
      - resourceRefId: example-markdown-combined
  resourcesRefs:
    items:
      - id: example-markdown-combined
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-combined
        namespace: krateo-system
        resource: markdowns
        verb: GET  
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-long-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Long content markdown'
    items:
      - resourceRefId: example-markdown-long
  resourcesRefs:
    items:
      - id: example-markdown-long
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-long
        namespace: krateo-system
        resource: markdowns
        verb: GET  
---
kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-markdown-table-panel
  namespace: krateo-system
spec:
  widgetData:
    actions: {}
    title: 'Markdown with table'
    items:
      - resourceRefId: example-markdown-table
  resourcesRefs:
    items:
      - id: example-markdown-table
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: example-markdown-table
        namespace: krateo-system
        resource: markdowns
        verb: GET  